<form method="post" action="{{ path('app_piezas_iphone_delete', {'id': piezas_iphone.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piezas_iphone.id) }}">
    <button class="btn">Delete</button>
</form>
